{
  "mcpServers": {
    "playwright": {
      "command": "npx",
      "args": [
        "@playwright/mcp@latest",
        "--config",
        "${CLAUDE_PLUGIN_ROOT}/playwright.config.json"
      ]
    }
  }
}
